function sumValues(a, b, add) {
    if (add) {
        const result = 0;
        result = a + b;
        console.log(result);
    } else return;

    console.log("final result: " + result);
}

sumValues(10, 10, true);